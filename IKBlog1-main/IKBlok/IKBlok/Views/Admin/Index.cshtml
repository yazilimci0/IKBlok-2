
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Adminler</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="#">Home</a></li>
                    <li class="breadcrumb-item active">Kullanıcılar</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<section class="content">
    <div class="container-fluid">
 @*        <p>
            <a asp-action="Create" class="btn btn-secondary">Create New</a>
        </p>

        <table id="myTable" class="display">
            <thead>
                <tr>
                    <th>
                        Admin Adı
                    </th>
                    <th>Edit</th>
                    <th>Details</th>
                    <th>Delete</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="w-75">
                            @Html.DisplayFor(modelItem => item.AdminAdi)
                        </td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.AdminId" class="btn btn-warning">Edit</a>
                        </td>
                        <td>
                            <a asp-action="Details" asp-route-id="@item.AdminId" class="btn btn-success">Details</a>
                        </td>
                        <td>
                            <a asp-action="Delete" asp-route-id="@item.AdminId" class="btn btn-danger">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <script>
            $(document).ready(function () {
                let table = new DataTable('#myTable');
            });
        </script>
 *@

    </div>
</section>

